/**
 * Scene registration.
 *
 * Every showcase scene is imported here for its side effect. Nothing else in
 * the application knows the scene set exists — the shell reads the registry.
 *
 * Adding a scene is one file plus one line here.
 */
import "./engine";
import "./production";
import "./timeline";
import "./stress";
